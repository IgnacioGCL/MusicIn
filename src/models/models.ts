export interface UserInfo {
    name: string;
    role: string;
    email: string;
    photoUrl: string;
}

export interface MessageInfo {
    photoUrl: string;
    name: string;
    role: string;
    text: string;
}